name: 📄 Generate PDFs from Markdown

on:
  push:
    branches: [ main ]
    paths:
      - 'docs/**/*.md'
      - '.github/workflows/pdf-generation.yml'
  workflow_dispatch:

jobs:
  generate-pdfs:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      - name: 🔧 Install Pandoc and Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y pandoc texlive-xetex texlive-latex-base texlive-latex-extra

      - name: 📂 Create Output Directories
        run: |
          mkdir -p docs/pdf/en
          mkdir -p docs/pdf/es

      - name: 📄 Generate English PDFs
        run: |
          for file in docs/en/sec-rules/*.md; do
            if [ -f "$file" ]; then
              filename=$(basename "$file" .md)
              output="docs/pdf/en/${filename}.pdf"
              echo "Converting $filename to PDF..."
              pandoc \
                --from markdown \
                --to pdf \
                --pdf-engine=xelatex \
                --variable mainfont="DejaVu Sans" \
                --variable monofont="DejaVu Sans Mono" \
                --variable fontsize=11pt \
                --variable geometry:margin=1in \
                --variable colorlinks=true \
                --variable linkcolor=blue \
                --variable urlcolor=blue \
                --toc \
                --toc-depth=2 \
                --number-sections \
                --output "$output" \
                "$file" || echo "Error converting $file"
            fi
          done
          echo "✅ English PDFs generated"

      - name: 📄 Generate Spanish PDFs
        run: |
          for file in docs/es/sec-rules/*.md; do
            if [ -f "$file" ]; then
              filename=$(basename "$file" .md)
              output="docs/pdf/es/${filename}.pdf"
              echo "Converting $filename to PDF..."
              pandoc \
                --from markdown \
                --to pdf \
                --pdf-engine=xelatex \
                --variable mainfont="DejaVu Sans" \
                --variable monofont="DejaVu Sans Mono" \
                --variable fontsize=11pt \
                --variable geometry:margin=1in \
                --variable colorlinks=true \
                --variable linkcolor=blue \
                --variable urlcolor=blue \
                --toc \
                --toc-depth=2 \
                --number-sections \
                --output "$output" \
                "$file" || echo "Error converting $file"
            fi
          done
          echo "✅ Spanish PDFs generated"

      - name: 📊 Verify PDF Generation
        run: |
          echo "📋 Files generated:"
          find docs/pdf -name "*.pdf" -type f | wc -l
          echo ""
          echo "📁 English PDFs:"
          ls -lh docs/pdf/en/ 2>/dev/null | tail -n +2 || echo "No files yet"
          echo ""
          echo "📁 Spanish PDFs:"
          ls -lh docs/pdf/es/ 2>/dev/null | tail -n +2 || echo "No files yet"

      - name: 📤 Upload Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: security-pdfs
          path: docs/pdf/
          retention-days: 30

      - name: 📤 Commit PDFs to Repository
        if: always()
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add docs/pdf/ 2>/dev/null || true
          if git diff --quiet --cached; then
            echo "✓ No PDF changes to commit"
          else
            git commit -m "docs(pdf): auto-generate PDFs from markdown [CI skip]"
            git push || echo "Note: Some files may not have changed"
          fi
